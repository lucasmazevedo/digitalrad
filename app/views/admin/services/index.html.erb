      <h1 class="page-header">Serviços <%= link_to 'Cadastrar Novo', new_admin_service_path, class: "btn btn-primary pull-right" %></h1>
            <div class="row">
                <div class="col-lg-12">
                    <div class="panel panel-default">
                        <div class="panel-heading">Lista </div>
                        <!-- /.panel-heading -->
                        <div class="panel-body">
                            <div class="table-responsive">
                                <table class="table table-striped table-bordered table-hover">
                                    <thead>
                                        <tr>
                                            <th>#</th>
                                            <th>Titulo</th>
                                            <th>Descrição</th>
                                            <th>Imagem</th>
                                            <th>Status</th>
                                            <th>Ações</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% @services.each do |service| %>
                                          <tr>
                                            <td><%= service.id %></td>
                                            <td><%= service.title %></td>
                                            <td><%= service.description %></td>
                                            <td><%= image_tag service.image, width: "50px" %></td>
                                            
                                            <td><%= service.statuses %></td>
                                            <td>
                                              <%= link_to 'Exibir', [:admin, service], class: "btn btn-info btn-sm" %>
                                              <%= link_to 'Editar', edit_admin_service_path(service), class: "btn btn-warning btn-sm" %>
                                              <%= link_to 'Excluir', [:admin, service], class: "btn btn-danger btn-sm", method: :delete, data: { confirm: 'Deseja deletar o registro?' } %>
                                            </td>
                                          </tr>
                                        <% end %>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                        <!-- /.panel-body -->
                    </div>
                    <!-- /.panel -->
                </div>
            </div>