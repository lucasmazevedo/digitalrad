<%= form_for([:admin, testemonial]) do |f| %>
  <% if testemonial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(testemonial.errors.count, "error") %> prohibited this testemonial from being saved:</h2>

      <ul>
      <% testemonial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
  <%= f.label :name %>
  <%= f.text_field :name, class: "form-control", placeholder: "Nome" %>
</div>

<div class="form-group">
  <%= f.label :organization %>
  <%= f.text_field :organization, class: "form-control", placeholder: "Organização/Empresa" %>
</div>

<div class="form-group">
  <%= f.label :description %>
  <%= f.text_field :description, class: "form-control", placeholder: "Texto" %>
</div>

<div class="form-group">
  <%= f.label :avatar %>
  <%= f.file_field :avatar, class: "form-control", placeholder: "Titulo" %>
</div>

 <div class="form-group">
    <%= f.label :status %>
    <%= f.check_box :status %>
  </div>

  <%= f.submit btn, class: "btn btn-primary" %>
<% end %>
