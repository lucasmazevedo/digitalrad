      <h1 class="page-header">Depoimentos <%= link_to 'Cadastrar Novo', new_admin_testemonial_path, class: "btn btn-primary pull-right" %></h1>
            <div class="row">
                <div class="col-lg-12">
                    <div class="panel panel-default">
                        <div class="panel-heading">Lista </div>
                        <!-- /.panel-heading -->
                        <div class="panel-body">
                            <div class="table-responsive">
                                <table class="table table-striped table-bordered table-hover">
                                    <thead>
                                        <tr>
                                            <th>#</th>
                                            <th>Titulo</th>
                                            <th>Organização</th>
                                            <th>Descrição</th>
                                            <th>Imagem</th>
                                            <th>Status</th>
                                            <th>Ações</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% @testemonials.each do |testemonial| %>
                                          <tr>
                                            <td><%= testemonial.id %></td>
                                            <td><%= testemonial.name %></td>
                                            <td><%= testemonial.organization %></td>
                                            <td><%= testemonial.description %></td>
                                            <td><%= image_tag testemonial.avatar, width: "50px" %></td>
                                            <td><%= testemonial.statuses %></td>
                                            <td>
                                              <%= link_to 'Exibir', [:admin, testemonial], class: "btn btn-info btn-sm" %>
                                              <%= link_to 'Editar', edit_admin_testemonial_path(testemonial), class: "btn btn-warning btn-sm" %>
                                              <%= link_to 'Excluir', [:admin, testemonial], class: "btn btn-danger btn-sm", method: :delete, data: { confirm: 'Deseja deletar o registro?' } %>
                                            </td>
                                          </tr>
                                        <% end %>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                        <!-- /.panel-body -->
                    </div>
                    <!-- /.panel -->
                </div>
            </div>